export function tone(kind:'scan'|'success'|'failure'){try{const C=window.AudioContext||(window as any).webkitAudioContext;const ctx=new C();const o=ctx.createOscillator();const g=ctx.createGain();o.connect(g);g.connect(ctx.destination);o.frequency.value=kind==='success'?880:kind==='failure'?220:660;g.gain.value=.08;o.start();setTimeout(()=>{o.stop();ctx.close()},kind==='scan'?90:220)}catch{}}
